#!/usr/bin/env node
var debug = require('debug')('quiz');
var app = require('../app');
// Mod 9 quiz 19 -  Conexiones seguras 
var fs = require("fs");
var https = require("https");

var options = {
    key: fs.readFileSync('cert/quiz_2015-key.pem').toString(),
    cert: fs.readFileSync('cert/quiz_2015-cert.pem').toString()
};

app.set('port', process.env.PORT || 3000);

var server = app.listen(app.get('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});

// Mod 9 quiz 19 -  Conexiones seguras 
var serverSecure = https.createServer(options, app);
serverSecure.listen(8443, function() {
  debug('Express server listening on port ' + server.address().port);
});
